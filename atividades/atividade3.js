let idade = 18;
let convite = 1;
let mensagem = convite >=1 ? "Acesso permitido" : "Acesso negado";

if (idade >= 18) {
    console.log("Você está autorizado!");
} else {
    console.log("Você não pode entrar!")
}